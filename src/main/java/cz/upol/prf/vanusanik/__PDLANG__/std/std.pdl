// Standard library main entry point module

module std() {

	foreign type System;
	
	foreign function systemInstance() rets System;
	
	foreign function nodeCurrentTimeMS(system : System) rets lng;  

}